# .env.local
# Copy this file to .env.local and set values for your environment.
#
# CRITICAL: NEXT_PUBLIC_API_URL must point to your backend port (5000),
# NOT the Next.js dev server port (3000).
# Without this, the pool socket connects to localhost:3000 and fails.

NEXT_PUBLIC_API_URL=http://localhost:5000
